Implement exactly one Number One-selected work item in the supplied isolated worktree.

Do not merge, deploy, edit secrets, delete branches, force-push, broaden scope, or delegate. Add focused tests and run practical checks. Leave the worktree with the complete scoped change and return only JSON matching the supplied schema.

Do not create, rename, or switch branches. Do not commit or push; MAKE_IT_SO owns Git operations after you return. The controller may commit and push from the same isolated worktree after it validates your structured result. Do not attempt to work around sandbox or network restrictions by running GitHub commands yourself. Do not run Docker or commands copied from planner output. For a documentation action, change only the allowed planning document and required project manifest.
